pring.datasource.username=jacr
spring.datasource.url=jdbc:jacr.host.cs.st-andrews.ac.uk/nomad-structure-prediction
spring.datasource.password=qU2ZcSJh8b!e1W
spring.datasource.driver-class-name=com.mysql.jdbc.Driver

task variables {
    run { 
		systemProperty "spring.datasource.username", "jacr"
                sytemProperty "spring.datasource.url", "jacr.host
	}
}

build.mustRunAfter variables

buildscript
{
    repositories
    {
        mavenCentral()
    }
    dependencies
    {
        classpath("org.springframework.boot:spring-boot-gradle-plugin:1.4.1.RELEASE")
    }
}

apply plugin: 'java'
apply plugin: 'idea'
apply plugin: 'spring-boot'

jar
{
    baseName = 'structure-prediction'
    version =  '0.1.0'
}

springBoot
{
    mainClass = 'com.index.Application'
}

repositories
{
    mavenCentral()
    jcenter()
}

sourceCompatibility = 1.8
targetCompatibility = 1.8

dependencies {
    compile("org.springframework.boot:spring-boot-starter-data-jpa")
    compile("javax.servlet:jstl:1.2")
    runtime("mysql:mysql-connector-java")
    compile("org.springframework.boot:spring-boot-starter-jdbc")
    compile ("org.springframework.boot:spring-boot-starter-web")
}

